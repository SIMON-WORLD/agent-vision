# 图片处理路由规则

- 当主模型支持原生图像输入（例如官方 Codex / GPT）时，直接读取图片，不要调用 `vision_bridge.py`。
- 当主模型是 DeepSeek 等纯文本模型时：
  - 用户提供图片路径：执行 `python vision_bridge.py see <path> -q "<问题>"`，把输出作为事实依据。
  - 用户粘贴图片且本地代理已运行：代理会自动把图片转成文字，正常回答即可。
  - 图片读取失败或显示“不支持图片格式”：不要猜测图片内容，按 SKILL.md 的步骤调用 `see`，并把失败信息告诉用户。
- 所有图片外发都必须遵守项目规则：默认发送到智谱免费视觉 API；用户可改用其他 OpenAI 兼容服务。
